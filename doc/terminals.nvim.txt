*terminals.nvim.txt*
                              For Neovim >= 0.8.0    Last change: 2026 July 16

==============================================================================
Table of Contents                           *terminals.nvim-table-of-contents*

1. terminals.nvim                              |terminals.nvim-terminals.nvim|
  - Requirements                  |terminals.nvim-terminals.nvim-requirements|
  - Installation                  |terminals.nvim-terminals.nvim-installation|
  - Setup                                |terminals.nvim-terminals.nvim-setup|
  - Default Keymaps            |terminals.nvim-terminals.nvim-default-keymaps|
  - Configuration                |terminals.nvim-terminals.nvim-configuration|
  - Commands                          |terminals.nvim-terminals.nvim-commands|
  - API                                    |terminals.nvim-terminals.nvim-api|
2. Links                                                |terminals.nvim-links|

==============================================================================
1. terminals.nvim                              *terminals.nvim-terminals.nvim*



A terminal manager for Neovim with up to 10 persistent terminal slots, a
tab-style header, and keyboard-driven navigation.


REQUIREMENTS                      *terminals.nvim-terminals.nvim-requirements*

- Neovim >= 0.8.0
- macOS (default keymaps use the `<D-…>` Cmd modifier)


INSTALLATION                      *terminals.nvim-terminals.nvim-installation*


LAZY.NVIM ~

>lua
    {
      "sassanh/terminals.nvim",
      config = function()
        require("terminals").setup()
      end,
    }
<


PACKER.NVIM ~

>lua
    use {
      "sassanh/terminals.nvim",
      config = function()
        require("terminals").setup()
      end,
    }
<


LUAROCKS ~

>sh
    luarocks install terminals.nvim
<


SETUP                                    *terminals.nvim-terminals.nvim-setup*

Call `setup()` to register keymaps and autocmds:

>lua
    require("terminals").setup()
<


DEFAULT KEYMAPS                *terminals.nvim-terminals.nvim-default-keymaps*

  Keymap              Action
  ------------------- ------------------------------------------------------
  <D-0> … <D-9>       Open or switch to terminal slot 0–9
  <D-BS>              Toggle the terminal window
  <D-h> / <D-l>       Move left/right between terminals
  <D-S-h> / <D-S-l>   Move the terminal buffer left/right
  <D-i>               Enter terminal input mode (all keys go to the shell)
  <D-S-i>             Leave terminal input mode
  <D-[>               Leave terminal mode
  <D-/> / <D-?>       Backward search in the terminal buffer
  <D-p> / <D-S-p>     Paste with p or P in the terminal buffer

CONFIGURATION                    *terminals.nvim-terminals.nvim-configuration*


KEYMAPS ~

All keymaps are configurable:

>lua
    require("terminals").setup({
      keys = {
        go_left = "<D-h>",
        go_right = "<D-l>",
        move_left = "<D-S-h>",
        move_right = "<D-S-l>",
        toggle = "<D-BS>",
        toggle_reverse_search = "<D-/>",
        focus = "<D-i>",
        unfocus = "<D-S-i>",
        leave = "<D-[>",
        paste = "<D-p>",
        paste_in_place = "<D-S-p>",
        modifier = "D",
      },
      preserved_keys = {},
    })
<

`modifier` is used to build the `<D-0>` … `<D-9>` slot keymaps. Set it to
match your platform’s super/meta modifier (for example `"D"` on macOS).

`preserved_keys` lets you keep specific terminal-mode keymaps when leaving
input mode.


WINDOW SIZE AND POSITION ~

`width`, `height`, `row`, and `col` control the terminal window layout. Each
accepts a fixed number or a percentage string such as `"80%"`.

>lua
    require("terminals").setup({
      width = "70%",
      height = 40,
      row = 0,
      col = "center",
    })
<

  Option   Default                    Description
  -------- -------------------------- --------------------------------------
  width    Scales with editor width   Window width in columns
  height   vim.o.lines - 1            Window height in lines
  row      0                          Distance from the top of the editor
  col      Horizontally centered      Distance from the left of the editor
For `row` and `col`, you can also use alignment keywords:

- `"center"` — center along that axis
- `"bottom"` — align to the bottom (`row` only)
- `"right"` — align to the right (`col` only)

Computed dimensions are clamped to stay on screen. Width is never less than 26
columns. Height is never less than 4 lines, which is the minimum needed to
render the border and tab bar. If the configured width is too narrow for the
full tab bar, tab padding is reduced before the existing tab-bar
scroll/truncation kicks in.


COMMANDS                              *terminals.nvim-terminals.nvim-commands*

- `:Terminal [cmd]` — open a terminal, optionally running `cmd`
- `:ToggleTerminal` — toggle the terminal window
- `:CloseTerminal` — close the terminal window


API                                        *terminals.nvim-terminals.nvim-api*

>lua
    require("terminals").activate_terminal({ id = 1 })
<

Options:

- `id` — terminal slot (0–9)
- `toggle` — toggle if already on the same slot (default: `true`)
- `append_mode` — start in insert mode (default: `true`)
- `args` — shell command to run when creating a new terminal

New terminals use your default shell unless `args` is passed or `shell` is set
in `setup()`.

>lua
    require("terminals").setup({
      shell = "/bin/bash",
    })
<

==============================================================================
2. Links                                                *terminals.nvim-links*

1. *GitHub Workflow Status*: https://img.shields.io/github/actions/workflow/status/sassanh/terminals.nvim/lint-test.yml?branch=main&style=for-the-badge
2. *Lua*: https://img.shields.io/badge/Made%20with%20Lua-blueviolet.svg?style=for-the-badge&logo=lua

Generated by panvimdoc <https://github.com/kdheepak/panvimdoc>

vim:tw=78:ts=8:noet:ft=help:norl:
